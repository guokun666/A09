<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.demo.dao.SectionPreditcDao">

	<!-- 前面是实体的字段名字，这个映射还要仔细问问 -->
    <resultMap type="com.boot.demo.entity.SectionPreditcEntity" id="SectionPreditcMap">

        <result property="line" column="line"/>
        <result property="flow_up" column="flow_up"/>
        <result property="flow_down" column="flow_down"/>
        <result property="time" column="time_slot"/>
        <result property="station" column="station_name"/>
        <result property="day" column="y_day"/>

    </resultMap>
    <select id="getSectionList" resultMap="SectionPreditcMap">
        SELECT flow_up,flow_down,station_name
        FROM g_1 WHERE time_slot = #{time} and
        station_name in (select station_name from station1 where line_name= #{singleline} )
        and y_day= #{day}
    </select>
    <select id="getTopUp" resultMap="SectionPreditcMap">
        SELECT station_name
        FROM g_1 WHERE time_slot = #{time} and
        station_name in (select station_name from station1 where line_name= #{singleline} ) and y_day= #{day}
        order by upward_flow desc limit 1
    </select>
    <select id="getTopDown" resultMap="SectionPreditcMap">
        SELECT station_name
        FROM g_1 WHERE time_slot = #{time} and
        station_name in (select station_name from station1 where line_name= #{singleline} ) and y_day= #{day}
        order by downward_flow desc limit 1
    </select>



</mapper>